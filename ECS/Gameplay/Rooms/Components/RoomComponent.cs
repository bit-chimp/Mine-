
//TODO : Compose rooms in better fashion, right now if I move the Room Prefab the children don't follow, only when I set Entities PositionComponent do they move
public sealed class RoomComponent : Entitas.IComponent
{
    //flag
}